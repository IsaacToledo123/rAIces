// Copia este archivo como app_secrets.dart y pon tus keys reales.
// app_secrets.dart está en .gitignore — nunca se sube al repo.
class AppSecrets {
  static const openAiKey = 'YOUR_OPENAI_API_KEY';
  static const anthropicKey = 'YOUR_ANTHROPIC_API_KEY';
}
